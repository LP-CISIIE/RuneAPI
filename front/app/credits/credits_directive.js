rune.directive('credits', ['$rootScope', function($rootScope) {
    // pour appeler les templates
    return {
        templateUrl: 'template/credits.html',
        link: function(scope) {

        }
    };
}]);